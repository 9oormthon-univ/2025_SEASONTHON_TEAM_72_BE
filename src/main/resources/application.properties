
server.port=8080
spring.jackson.property-naming-strategy=SNAKE_CASE

# ?????? (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/Pizza?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA / Hibernate
# validate : ?????? ?? ? ???? ??? ??, ???? ?? ???? dksgka
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Firebase (Admin SDK)
# ??? ?? JSON ??(resources/ ??) ? ?? ??? ??? ??
firebase.admin.service-account=${FIREBASE_SERVICE_ACCOUNT_PATH}

# Firebase Web API ? (REST ????)
firebase.api-key=${FIREBASE_API_KEY}

# JWT
# ??? ??? (????? ?? ??)
jwt.secret=${JWT_SECRET}
# ?? ???? (???) -> 604,800,000 = 7?
jwt.expiration=${JWT_EXPIRATION}

# =========================================
# Cloudinary API ??
# =========================================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}
cloudinary.upload-preset=${CLOUDINARY_UPLOAD_PRESET}
=======
spring.application.name=pizza
